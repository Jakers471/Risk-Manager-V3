Agent A — Engine/Enforcer
Owns: Monitor loop; enforce flatten/reduce/cancel/lockout; write runtime/events/<cid>.json; honor runtime/lock.json.
Out of scope: Policy logic (B), external API plumbing (C).
